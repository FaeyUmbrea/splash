{{!-- Presets are authored from the splash editor; edit mode only carries the core header (rename). --}}
<section class="journal-page-content">
	<p class="notification info">
		This is a Splash preset ({{ preset.kindLabel }}). Presets are created and applied from the Splash editor —
		here you can rename it. To change its contents, apply it to a splash, edit there, and save a new preset.
	</p>
</section>
